# Spam-Email-Detector
A Java-based spam email detection system that uses natural language processing and machine learning to classify emails as spam or legitimate. It features data preprocessing, text tokenization, model training with Naive Bayes, and a user interface for testing new messages.
